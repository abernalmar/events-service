build:
  events-service:
    context: .

deploy:
  - helm upgrade --install events-service chart
    --set image.repository=${OKTETO_BUILD_EVENTHUB_EVENTS_IMAGE}

dev:
  events-service:
    command: npm start
    sync:
      - .:/app