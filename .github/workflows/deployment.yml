name: Okteto Deployment

on:
  push:
    branches: [main]
    
jobs:
  test:
    uses:  abernalmar/events-service/.github/workflows/tests.yml@main
    
  deploy:
    needs:  test
    runs-on: ubuntu-latest

    steps:
      
      - uses: actions/checkout@v2
