build:
  events-service:
    context: .

deploy:
  - helm upgrade --install events-service chart
    --set events.image=${OKTETO_BUILD_EVENTHUB_EVENTS_IMAGE}

dev:
  events-service:
    command: npm start
    sync:
      - .:/app